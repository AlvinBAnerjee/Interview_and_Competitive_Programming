package MachineCoding_LLD.LLD_Interview_Problems._01_Easy_ParkingLotSystem.model;

/**
 * The kinds of vehicles the lot supports. A slot serves exactly one type, and a vehicle
 * can only take a slot of its own type.
 */
public enum VehicleType {
    MOTORCYCLE,
    CAR,
    TRUCK
}
